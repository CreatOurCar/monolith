server {
  listen 7000;
  port_in_redirect off;

  root /var/public;
  index index.html;
  charset utf-8;

  location /docs {
    index index.html;
    try_files $uri $uri/ /docs/index.html;
  }

  location / {
    try_files $uri /index.html;
  }
}
